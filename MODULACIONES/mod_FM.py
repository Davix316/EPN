import matplotlib.pyplot as plt
import numpy as np
from numpy import*
from math import*

#### ................MODULACION FM............

"""t=np.arange(0,0.01,10**(-5.7))
fm1=10**3
x=2*np.sin(2*pi*fm1*t)
fc=20
fs=100*(10**6)
F=0.7*fc
y=np.sin(2*pi*fc+(F*sin(2*pi*fm1*t)))
plt.subplot(311)
plt.plot(t,x,linewidth=0.4)
plt.title("señal de mensaje ",fontsize=12)
plt.subplot(312)
r=2*np.sin(fc*pi*fm1*t)
plt.plot(t,r,linewidth=0.4)
plt.title("funcion",fontsize=12)
plt.subplot(312)
plt.title("modulacion FM",fontsize=12)
plt.plot(t,y,linewidth=0.04)"""
